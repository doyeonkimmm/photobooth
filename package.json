{
  "name": "mood-fourcut",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build --base=/photobooth/",
    "preview": "vite preview --host 0.0.0.0 --base=/photobooth/"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
